enum TreatmentStatus { active, completed, cancelled }

// Payment type enums
enum PaymentType { installment, partial, full }

// Patient treatment status enums
enum PatientTreatmentStatus { active, completed, cancelled }